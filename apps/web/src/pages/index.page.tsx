// The app's landing view is the your-move dashboard — the human court of the
// status protocol. It lives in the projects feature; this file only routes.
import { YourMovePage } from '@/features/projects';

export default YourMovePage;
